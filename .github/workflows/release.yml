name: Release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      Api_Key: ${{secrets.API_KEY}}

    steps:
      - uses: actions/checkout@v1
      - name: Publish
        run: |
          pwsh -Command "write-host 'Hello'"
